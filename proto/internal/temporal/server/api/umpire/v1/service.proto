syntax = "proto3";

package temporal.server.api.umpire.v1;

import "temporal/server/api/umpire/v1/portable_test_plan.proto";

option go_package = "go.temporal.io/server/api/umpire/v1;umpire";

service UmpireExecutor {
  // (-- api-linter: core::0136::verb-noun=disabled
  //     aip.dev/not-precedent: Execute is the established Umpire operation name. --)
  // (-- api-linter: core::0127::http-annotation=disabled
  //     aip.dev/not-precedent: This internal successor is intentionally gRPC-only. --)
  rpc Execute(PortableTestPlan) returns (ExecutionResult) {}
}
