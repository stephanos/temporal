# Generated by "buf config migrate-v1beta1". Edit as necessary, and
# remove this comment when you're finished.
#
# This module represents the "internal" root found in
# the previous configuration.
version: v1
deps:
  - buf.build/googleapis/googleapis
breaking:
  use:
    - WIRE
  ignore:
    # The testpilot protocol is an internal test-only wire with no compatibility
    # promise; its messages are renamed wholesale rather than versioned.
    - temporal/server/api/testpilot
lint:
  use:
    - DEFAULT
  except:
    - PACKAGE_SAME_JAVA_MULTIPLE_FILES
    - PACKAGE_SAME_JAVA_PACKAGE
