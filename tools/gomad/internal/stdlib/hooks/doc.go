/*
Package hooks is the stable wrapper layer between the supported Go standard
library and the gomad runtime. The gomad translator replaces runtime linknames
in the standard library with functions from this package.
*/
package hooks
