name: Umpire

on:
  pull_request:
    paths:
      - ".github/workflows/umpire.yml"
      - "model/**"
      - "tools/umpire/**"
      - "Makefile"
      - "go.mod"
      - "go.sum"
  push:
    branches:
      - main
      - stephanos/umpire
    paths:
      - ".github/workflows/umpire.yml"
      - "model/**"
      - "tools/umpire/**"
      - "Makefile"
      - "go.mod"
      - "go.sum"

permissions:
  contents: read

concurrency:
  group: umpire-${{ github.head_ref || github.run_id }}
  cancel-in-progress: true

jobs:
  portability:
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
      - uses: actions/setup-go@4a3601121dd01d1626a1e23e37211e3254c1c06c # v6.4.0
        with:
          go-version-file: "go.mod"
          check-latest: false
          cache: false
      - uses: jdx/mise-action@dba19683ed58901619b14f395a24841710cb4925 # v4.1.0
        with:
          working_directory: model
          cache: false
      - name: Run the ordinary Umpire test
        run: mise exec -- go test -count=1 -tags test_dep ./tools/umpire/temporal/nexus/... -run '^TestHermeticCIPortability$'
