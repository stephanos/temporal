---
satisfies: [R2, R7]
---

# fn-28-portable-evaluation-contract-and.3 Lower one checked Lean Test into a closed portable contract
## Description

Add a Lean-owned lowering pass that specializes the selected caller-closure Test, Observation, Implementation Link, Property, Limits, Known Gaps, and bindings into the protobuf contract vocabulary and emits canonical ProtoJSON.

**Size:** L
**Files:** `model/Umpire/Artifact/PortableEvaluationContract.lean`, `model/Umpire/Artifact/Tests/PortableEvaluationContract.lean`, `model/Temporal/Tool/PortableEvaluationContract.lean`, `model/Temporal/Tool/PortableEvaluationContractTests.lean`, `model/lakefile.toml`
**Touches:** [`model/Umpire/Artifact/PortableEvaluationContract.lean`, `model/Umpire/Artifact/Tests/PortableEvaluationContract.lean`, `model/Temporal/Tool/PortableEvaluationContract.lean`, `model/Temporal/Tool/PortableEvaluationContractTests.lean`, `model/lakefile.toml`]

### Approach
- Lower only checked model values; retain every source Definition ID, Behavior Fingerprint, Artifact binding, Limit, Known Gap, and source location required to audit the output.
- Specialize away model-registry and whole-world dependencies. Return a named non-portable outcome when a checked semantic construct cannot be represented by the closed IR.
- Emit canonical ProtoJSON as a build handoff only; no Go callback or packer decision may supply missing semantic data.

### Investigation targets

**Required** (read before coding):
- `.plans/LEAN_GUIDELINES.md`, parent spec, and task `.1` schema.
- `model/Temporal/Tool/RunEvaluation.lean`, Observation Evaluation, Implementation Link, Property verdict, and Artifact canonical renderers.
- Existing generated-view and checksum/fingerprint tests.

## Acceptance
- [ ] The exact normal and duplicate-delivery Tests lower deterministically into complete closed contracts.
- [ ] Unsupported semantics report non-portable with explicit source identity and cannot emit a passing contract.
- [ ] Mutation tests prove Definition IDs, fingerprints, clauses, closure, Limits, and Known Gaps affect the generated bytes as intended.

## Done summary
Implemented the Lean-owned closed portable Evaluation Contract IR, canonical ProtoJSON renderer, and deterministic normal/duplicate caller-closure lowering with typed non-portable rejection. Exact tests cover source identities, authoritative fingerprints, provenance, byte-affecting mutations, and Lean-to-Go canonical round-trip/structural packing.

Verification passed for protobuf generation, the focused Lean target, Go structural packing, the tagged integration command, and `make lint-model`; post-edit checks reproduced only the pre-edit/future-task baselines: absent `portableevaluation`/`executor` packages, the existing `KnownGaps.lean:296` owner rejection, and the existing 1,373 `lint-code` findings. Review fixes preserved duplicate observed-link identity, reused canonical Implementation Link definition fingerprints, and closed definition-owner provenance; memory capture was skipped because repository memory is not initialized.

stage: impl-review - ran [2026-09-01T18:55:26Z..2026-09-01T19:11:18Z]
## Evidence
- Commits: 19ed3f44c729326e9cd43270be3f68035308a830, 645f481efe2a66110ced5a861e919b718e69cf7f
- Tests: make proto (pass; known generator drift restored), cd model && mise exec -- lake build Temporal.Tool.PortableEvaluationContractTests (pass), cd model && mise exec -- lake env lean ../.flow/tmp/fn28_emit_contracts.lean; go run ./.flow/tmp/fn28_check_contracts.go (pass: exact ProtoJSON round-trip and evaluationcontract.Pack for normal and duplicate), go test -count=1 -tags test_dep ./tools/umpire/evaluationcontract/... ./tools/umpire/portableevaluation/... ./tools/umpire/executor/... (evaluationcontract pass; baseline red because future-task portableevaluation/executor paths do not exist), go test -count=1 -tags 'test_dep integration' ./tests -run '^TestUmpirePortableCanaryExecutor$' (pass; no tests to run at this task stage), make lint-model (pass), make umpire-check-regression (baseline/post-edit inherited red: model/Umpire/SemanticInventory/KnownGaps.lean:296 rejects existing owner Temporal.Tool.RunEvaluation), make lint-code (baseline/post-edit inherited red: 1373 findings; formatter side effects restored), git diff --check (pass)
- PRs: