---
satisfies: [R2, R3, R4, R5, R6]
---
# fn-25-optional-callerclosure-veil-binding-and.5 Build the focused mutation and isolation verification matrix

**Size:** L
**Files:** `model/Temporal/Feature/Nexus/CallerClosure/VeilTests.lean`, `model/TemporalVeilTests.lean`, formal receipt fixtures and import guards
**Touches:** v1/v2 canonical fixtures, relation/declaration mutation matrix, negative-control replay, optional import isolation

## Description
In adopt mode, add the non-default TemporalVeilTests aggregate and focused tests for establishment, the distinct test-only negative binding, bidirectional relation/table/declaration-digest sensitivity, identity/crossing/staleness, bounds, selected capability/trust, injective lowering, replay disagreement, constructor anti-forgery, unchanged v1 bytes, deterministic v2 bytes/evidence order, and import isolation. Run ordinary Umpire/Temporal/native verify/inspect/regression checks before and after. In defer mode, verify the exact frozen decision plus absence of dependency, manifest entry, sources, aggregate, executable, Make target, model docs, or semantic claim while running the same native/default checks.

## Acceptance
The selected branch's full positive, negative, mutation, structural-expression/axiom digest, capability/trust, canonical replay, v1 compatibility, v2 canonicalization, and isolation matrix passes. Default targets and current regression fixtures do not import optional code or change semantic bytes. Source/import guards keep reusable Umpire domain-neutral, generated projections unchanged, runtime/production untouched, and existing comments preserved.

## Done summary
TBD

## Evidence
- Commits:
- Tests:
- PRs:
