# fn-27-hermetic-ci-execution-and-qualification.2 Bind generated Go tests to the disposable CI runner

## Description
Run the generated ordinary Go test through the existing invocation-owned runner using fixed loopback bindings, concurrency one, declared phase and Evidence Limits, and deterministic participant/environment cleanup. Accept no external endpoint, credential, arbitrary executable, plugin, or undeclared network authority.

## Acceptance
- [ ] The generated test uses the shared runner and one bounded disposable loopback environment.
- [ ] Cancellation and every failure path kill/reap participants and prove cleanup.
- [ ] Limit N/N+1 and authority-leak fixtures fail at the runner boundary.

## Done summary
The generated `TestHermeticCIPortability` now runs the exact admitted input through the shared invocation-owned loopback runner, binds the generated authority capability set before participant construction, and asserts five successful phases with complete zero-handle cleanup. Independent N/N+1 and forbidden-authority fixtures fail at the runner boundary, while the exhaustive engine and local lifecycle suites prove cancellation and every failure path isolate, kill/reap, and clean up participants.

Baseline note: the unqualified direct Go Quick commands were inherited-red on this Darwin host because `mise` selects Lean's SDK-less bundled clang and the default `/var` temp alias violates physical containment; the exact commands pass with the repository-established Xcode compiler/SDK and physical `TMPDIR` bindings. Repository-wide `make lint-code` remains inherited-red at untouched `tools/umpire/runtime/errors.go:60`; task-base golangci reports 0 issues.

stage: impl-review - ran [2026-08-31T17:07:39Z..2026-08-31T17:13:19Z]
stage: plan-sync - skipped(config: planSync.enabled != true)
## Evidence
- Commits: 70638c0a2123e5c79411f7f5889e3cecff656771
- Tests: baseline: green - cd model && mise exec -- lake build Temporal.Tool.RunEvaluationTests, baseline: red inherited - mise exec -- go test -count=1 ./tools/umpire/runtime/... ./tools/umpire/runevaluation/... ./tools/umpire/temporal/nexus/... (mise selected Lean bundled clang without macOS SDK; default /var temp alias also failed physical containment), baseline: red inherited - mise exec -- go test -count=1 ./tools/umpire/temporal/nexus/... -run '^TestHermeticCIPortability$' (same Darwin toolchain/temp-root causes), baseline: green - mise exec -- make umpire-check-regression, STRICT_RED: go test -count=1 -tags test_dep ./tools/umpire/cmd/umpire-gen-tests-go - generated TestHermeticCIPortability did not invoke runner.Run, STRICT_RED: go test -count=1 -tags test_dep ./tools/umpire/runner -run '^TestRunRejectsAuthorityLeakBeforeParticipantConstruction$' - leaked checked authority reached participant construction, mise exec -- go test -count=1 -tags test_dep ./tools/umpire/runner -run '^(TestRunRejectsAuthorityLeakBeforeParticipantConstruction|TestRunRejectsLimitNPlusOneBeforeAdapterConstruction|TestRunPassesTheExactAdmittedSetToTheAdapter)$', mise exec -- go test -count=1 -tags test_dep ./tools/umpire/cmd/umpire-gen-tests-go, mise exec -- go test -count=1 -tags test_dep ./tools/umpire/temporal/nexus -run '^TestHermeticCIPortability$', mise exec -- go test -count=1 -tags test_dep ./tools/umpire/internal/runtimeengine ./tools/umpire/temporal/local, mise exec -- go test -count=1 -tags test_dep ./tools/umpire/runner ./tools/umpire/cmd/umpire-gen-tests-go ./tools/umpire/temporal/nexus, cd model && mise exec -- lake build Temporal.Tool.RunEvaluationTests, mise exec -- go test -count=1 ./tools/umpire/runtime/... ./tools/umpire/runevaluation/... ./tools/umpire/temporal/nexus/... (GREEN with repository-established Xcode CC/CXX/SDKROOT and physical TMPDIR), mise exec -- go test -count=1 ./tools/umpire/temporal/nexus/... -run '^TestHermeticCIPortability$' (GREEN with repository-established Xcode CC/CXX/SDKROOT and physical TMPDIR), mise exec -- make umpire-check-regression, GOLANGCI_LINT_BASE_REV=f609dff1663003211244cda4fa4bd3dfa35df026 GOLANGCI_LINT_FIX=false mise exec -- make lint-code (golangci: 0 task-diff issues; inherited tools/umpire/runtime/errors.go:60:9 et:unw+), git diff --check, impl-review codex session 01a058ca-92e0-79e1-9e7f-2890a1ca56d2: SHIP
- PRs:
