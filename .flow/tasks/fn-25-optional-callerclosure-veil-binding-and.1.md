---
satisfies: [R1, R6]
---
# fn-25-optional-callerclosure-veil-binding-and.1 Admit the compatibility receipt and freeze the implementation branch

**Size:** S
**Files:** none (Flow task evidence only)
**Touches:** none

## Description
Run the completed fn-23 opt-in gate once, capture stdout outside the repository, and strictly admit the canonical umpire-veil-compatibility/v1 receipt. Recompute fn-23's normalized receipt-identity preimage, verify the resulting decisionStableDigest equals receiptIdentity, and verify the current Lean toolchain, selected candidate/commit, closure identity, complete solverModes capability array, trust capabilities, and completed decision. Record exactly one umpire-veil-gate-binding/v1 branch record in Flow task evidence without raw host or measurement fields. In adopt mode retain the complete capability array and select commandSolverMode/commandTrust by kernel, reconstructed-solver, trusted-solver, testing, then canonical mode-name order; select defer only for a valid defer-incompatible or inconclusive receipt. A status-1, stale, malformed, crossed, or incomplete receipt blocks the task. The selected mode is immutable for the rest of fn25.

## Acceptance
The task evidence contains the exact closed branch record, admitted receipt identity, and equal normalized decisionStableDigest, with raw host/measurement fields absent. Adopt has non-null selected candidate/commit/closure, the unchanged complete solver capability array, and deterministically selected command mode/trust matching one exact entry; defer has an empty capability array, null selected fields, and the exact defer/inconclusive decision. No repository model, Lake, Make, or product artifact change is made by this task, and no later task may rerun the gate to switch modes.

## Done summary
TBD

## Evidence
- Commits:
- Tests:
- PRs:
