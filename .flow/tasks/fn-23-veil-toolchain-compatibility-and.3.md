---
satisfies: [R1, R3, R4]
---
# fn-23-veil-toolchain-compatibility-and.3 Run the bounded current-toolchain compatibility matrix

## Description
Build the acquisition/execution deep module around the frozen manifest and handwritten probe. Complete the allowlisted acquisition stage, chroot/mount only the verified runtime rootfs, source/npm/solver, and Lean/Zig/Node bundle closure, then execute a fixed temporary Lake overlay under Lean 4.33.1 with network and ambient runtime/compiler/sysroot/linker paths absent, filtered environment, the spec's exact aggregate/process-tree bounds, and no host checkout writes. This task owns candidate iteration and every raw observation: cold build, warm build, all three focused runs, positive/mutation probes, named solver/trust probes, descendant metrics, threshold inputs, normalized phase/result codes, progress callbacks, cancellation/reaping, and cleanup. Stop after the first fully observed compatible candidate; otherwise retain complete results for both. Provide package-private fake process/network/resource seams with independent phase/error matrices.

**Size:** M
**Files:** `tools/umpire/veilcompat/runner.go`, `tools/umpire/veilcompat/sandbox.go`, `tools/umpire/veilcompat/runner_test.go`, `tools/umpire/veilcompat/sandbox_test.go`
**Touches:** [tools/umpire/veilcompat/runner.go, tools/umpire/veilcompat/sandbox.go, tools/umpire/veilcompat/runner_test.go, tools/umpire/veilcompat/sandbox_test.go]

## Acceptance
The runner returns complete candidate-scoped raw phase results and all three focused observations needed by the pure decision layer; preview-incompatible/main-compatible reaches and fully observes main, while preview-compatible stops without starting main. Every exact dependency/tool revision, rootfs/bundle/tree/wrapper identity, ELF/compiler preflight result, and trust capability is retained. Source patching, cache miss/network during build, ambient interpreter/runtime/header/resource/crt/library/linker/subtool resolution, current-toolchain drift, any numeric N+1, timeout, cancellation, solver failure, child leak, or cleanup uncertainty follows the exact tooling/incompatibility boundary and can never yield compatible status.

## Done summary
TBD

## Evidence
- Commits:
- Tests:
- PRs:
