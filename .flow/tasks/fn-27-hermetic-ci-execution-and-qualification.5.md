# fn-27-hermetic-ci-execution-and-qualification.5 Compose the hermetic runner and shared Run Evaluation path

## Description
Compose strict v2 admission, the shared disposable runner, admitted Raw Evidence, and canonical Run Evaluation in one bounded test-facing path. Validate all inputs before environment creation, preserve status separation, and expose no Claim Assessment, Evaluation Profile, Evaluation Receipt, provenance Artifact, or new artifact-set version.

## Acceptance
- [ ] One valid bounded run traverses admission, runner, Run Evaluation, and cleanup exactly once.
- [ ] Cancellation, tooling failure, semantic non-success, and cleanup failure retain exact independent outcomes.
- [ ] No profile/policy selector, second publisher, or CI-specific semantic API exists.

## Done summary
Composed the generated hermetic test-facing path across strict admission, the disposable runner, canonical Run Evaluation, and cleanup while retaining execution, evaluation, and tooling outcomes independently. The review fix preserves validated semantic exit-2 results from the Make-backed evaluator; all four final-diff Quicks passed before review and again during terminal verification, and nonmutating lint reports zero task-diff issues with only the inherited `tools/umpire/runtime/errors.go:60` `et:unw+` finding.

baseline: green with the repository-established Darwin toolchain environment (the unprefixed Go Quicks inherit the local missing-`stddef.h` environment failure)
memory capture: skipped(error: Flow memory is not initialized)
stage: impl-review - ran [2026-08-31T20:14:15Z..2026-08-31T20:25:19Z]
## Evidence
- Commits: ae8a15f219b282f6d0226e8c83926958a8804a1c, 8aeed713d18a917f5e7efe38feb97f44c854a225
- Tests: baseline: green - cd model && mise exec -- lake build Temporal.Tool.RunEvaluationTests, baseline: green with repository-established Darwin toolchain - GOFLAGS=-tags=test_dep CC=$(xcrun --find clang) CXX=$(xcrun --find clang++) SDKROOT=$(xcrun --show-sdk-path) TMPDIR=$(pwd -P)/.flow/tmp/go-tmp mise exec -- go test -count=1 ./tools/umpire/runtime/... ./tools/umpire/runevaluation/... ./tools/umpire/temporal/nexus/... (unprefixed command inherited missing-stddef.h environment failure), baseline: green with repository-established Darwin toolchain - GOFLAGS=-tags=test_dep CC=$(xcrun --find clang) CXX=$(xcrun --find clang++) SDKROOT=$(xcrun --show-sdk-path) TMPDIR=$(pwd -P)/.flow/tmp/go-tmp mise exec -- go test -count=1 ./tools/umpire/temporal/nexus/... -run '^TestHermeticCIPortability$' (unprefixed command inherited missing-stddef.h environment failure), baseline: green - mise exec -- make umpire-check-regression, STRICT_RED: GOFLAGS=-tags=test_dep CC=$(xcrun --find clang) CXX=$(xcrun --find clang++) SDKROOT=$(xcrun --show-sdk-path) TMPDIR=$(pwd -P)/.flow/tmp/go-tmp mise exec -- go test -count=1 ./tools/umpire/temporal/nexus -run '^TestCallerClosurePath' (missing private composed path; /tmp/fn27-5-strict-red.log), STRICT_RED_REVIEW_FIX: GOFLAGS=-tags=test_dep CC=$(xcrun --find clang) CXX=$(xcrun --find clang++) SDKROOT=$(xcrun --show-sdk-path) TMPDIR=$(pwd -P)/.flow/tmp/go-tmp mise exec -- go test -count=1 ./tools/umpire/temporal/nexus -run '^TestCallerClosureEvaluationPreservesSemanticNonSuccess$' (Run Evaluation emitted stderr; /tmp/fn27-5-review-fix-red.log), GOFLAGS=-tags=test_dep CC=$(xcrun --find clang) CXX=$(xcrun --find clang++) SDKROOT=$(xcrun --show-sdk-path) TMPDIR=$(pwd -P)/.flow/tmp/go-tmp mise exec -- go test -count=1 ./tools/umpire/cmd/umpire-gen-tests-go ./tools/umpire/temporal/nexus -run '^(TestRenderGeneratedRunnerTestPinsHermeticSubjectBeforeRuntimeIO|TestRenderGeneratedRunnerTestMatchesTheCheckedInOrdinaryGoTest|TestCallerClosureEvaluationPreservesSemanticNonSuccess)$' (/tmp/fn27-5-review-fix-focused-green.log), cd model && mise exec -- lake build Temporal.Tool.RunEvaluationTests (/tmp/fn27-5-verify-lean.log), GOFLAGS=-tags=test_dep CC=$(xcrun --find clang) CXX=$(xcrun --find clang++) SDKROOT=$(xcrun --show-sdk-path) TMPDIR=$(pwd -P)/.flow/tmp/go-tmp mise exec -- go test -count=1 ./tools/umpire/runtime/... ./tools/umpire/runevaluation/... ./tools/umpire/temporal/nexus/... (/tmp/fn27-5-verify-go.log), GOFLAGS=-tags=test_dep CC=$(xcrun --find clang) CXX=$(xcrun --find clang++) SDKROOT=$(xcrun --show-sdk-path) TMPDIR=$(pwd -P)/.flow/tmp/go-tmp mise exec -- go test -count=1 ./tools/umpire/temporal/nexus/... -run '^TestHermeticCIPortability$' (/tmp/fn27-5-verify-hermetic.log), GOFLAGS=-tags=test_dep CC=$(xcrun --find clang) CXX=$(xcrun --find clang++) SDKROOT=$(xcrun --show-sdk-path) TMPDIR=$(pwd -P)/.flow/tmp/go-tmp mise exec -- make umpire-check-regression (/tmp/fn27-5-verify-regression.log), GOLANGCI_LINT_BASE_REV=ef784de087da2026b579dadfadf021ecef3bdb9d GOLANGCI_LINT_FIX=false mise exec -- make lint-code (golangci: 0 task-diff issues; INHERITED_RED: tools/umpire/runtime/errors.go:60:9 et:unw+; /tmp/fn27-5-verify-lint.log), git diff --check
- PRs: