# fn-27-hermetic-ci-execution-and-qualification.4 Prove Artifact Checksum and Behavior Fingerprint parity

## Description
Add independent golden and mutation checks proving the local and CI subjects have identical v2 bytes, Artifact Checksum, Definition IDs, and Behavior Fingerprints. Allow only declared runtime transport identities to differ; reject semantic recompilation, format crossing, incomplete closure, and nondeterministic generation.

## Acceptance
- [ ] Stable Artifact and model identities are identical across local and CI use.
- [ ] Each representative byte, checksum, fingerprint, version, and closure mutation is rejected.
- [ ] Tests do not share the production checksum/fingerprint oracle.

## Done summary
Added independent subject-parity tests proving that the local and CI caller-closure inputs are byte-identical and match literal stable IDs, fingerprints, versions, and independently computed checksums; focused mutations reject byte/checksum/fingerprint/version/closure/recompilation drift without sharing the production checksum oracle. The final Quick gates pass under the repository-established Xcode toolchain; one pre-existing Nexus causal-event ordering flake passed in isolation and on the full rerun, while nonmutating lint reports zero task-diff issues and only the inherited `tools/umpire/runtime/errors.go:60` `et:unw+` finding.

stage: impl-review - ran [2026-08-31T18:38:34Z..2026-08-31T18:42:42Z]
stage: plan-sync - skipped(config: planSync.enabled != true)
## Evidence
- Commits: 1094f7b9cf9db2b3f6e8f9fb6286f08a2ea184a2, fe40e940365baec78d22dc84dc1f6a93d52f0f8b
- Tests: baseline: green - cd model && mise exec -- lake build Temporal.Tool.RunEvaluationTests, baseline: red inherited - mise exec -- go test -count=1 ./tools/umpire/runtime/... ./tools/umpire/runevaluation/... ./tools/umpire/temporal/nexus/... (mise-selected Lean clang lacked the macOS SDK; green with repository-established Xcode CC/CXX/SDKROOT, -tags test_dep, and physical TMPDIR), baseline: red inherited - mise exec -- go test -count=1 ./tools/umpire/temporal/nexus/... -run '^TestHermeticCIPortability$' (same Darwin toolchain cause; green with repository-established Xcode CC/CXX/SDKROOT, -tags test_dep, and physical TMPDIR), GATE_SKIPPED:unittest:green-receipt 62eb6a03 - baseline reused from prior post-gate pass, CC=$(xcrun --find clang) CXX=$(xcrun --find clang++) SDKROOT=$(xcrun --show-sdk-path) TMPDIR=$(pwd -P)/.flow/tmp/go-tmp mise exec -- go test -tags test_dep -count=1 ./tools/umpire/runevaluation -run '^TestCheckSubject', CC=$(xcrun --find clang) CXX=$(xcrun --find clang++) SDKROOT=$(xcrun --show-sdk-path) TMPDIR=$(pwd -P)/.flow/tmp/go-tmp mise exec -- go test -tags test_dep -count=1 ./tools/umpire/cmd/umpire-gen-tests-go -run '^TestRunRegeneratesOnlyTheDeterministicGoTest$', CC=$(xcrun --find clang) CXX=$(xcrun --find clang++) SDKROOT=$(xcrun --show-sdk-path) TMPDIR=$(pwd -P)/.flow/tmp/go-tmp mise exec -- go test -tags test_dep -count=1 -race ./tools/umpire/runevaluation -run '^TestCheckSubject', cd model && mise exec -- lake build Temporal.Tool.RunEvaluationTests, CC=$(xcrun --find clang) CXX=$(xcrun --find clang++) SDKROOT=$(xcrun --show-sdk-path) TMPDIR=$(pwd -P)/.flow/tmp/go-tmp mise exec -- go test -tags test_dep -count=1 ./tools/umpire/runtime/... ./tools/umpire/runevaluation/... ./tools/umpire/temporal/nexus/..., INHERITED_FLAKE: first final broad Go run observed Nexus causal event ID 10/14 mismatch; the isolated failing test and the complete broad rerun both passed, CC=$(xcrun --find clang) CXX=$(xcrun --find clang++) SDKROOT=$(xcrun --show-sdk-path) TMPDIR=$(pwd -P)/.flow/tmp/go-tmp mise exec -- go test -tags test_dep -count=1 ./tools/umpire/temporal/nexus/... -run '^TestHermeticCIPortability$', mise exec -- make umpire-check-regression, GOLANGCI_LINT_BASE_REV=65936d299ed62885b66df671d1673a2c4bb0b75a GOLANGCI_LINT_FIX=false mise exec -- make lint-code (golangci: 0 task-diff issues; INHERITED_RED: tools/umpire/runtime/errors.go:60:9 et:unw+), git diff --check
- PRs:
