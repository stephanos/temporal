---
satisfies: [R3, R4, R6, R7]
---
# fn-69-extract-testpilot-from-umpire.6 Move the functional Temporal Driver to testcore

## Description
Move the complete Temporal environment implementation and live success consumer into the functional test-core owner, preserving separate server/worker authority and the full lifecycle corpus (R3, R4, R6, R7).

**Size:** L — cohesive adapter move; server, worker, delivery, composite, and coupled tests share internal visibility and lifecycle state
**Files:** `tests/testcore/testpilot/**`, former `tools/umpire/temporal/**` including old fixtures, pre-generated Testpilot fixtures, `tests/umpire4*_test.go`, `.github/workflows/umpire.yml`, `Makefile`, migration ledger
**Touches:** [tests/testcore/testpilot/**, tools/umpire/temporal/**, tests/umpire4*_test.go, .github/workflows/umpire.yml, Makefile, .flow/artifacts/fn-69-extract-testpilot-from-umpire/migration-ledger.md]

### Approach
- Move delivery, server, worker, composite Driver, fixtures, tests, and explanatory comments together; map `tests/testcore.TestEnv` into the Driver without moving cluster authority into common Testpilot.
- Retain the task-5 Testpilot fixtures at their final destination, remove the superseded Umpire fixtures with the old adapter path, and verify descriptor, type URL, embedded `NamedType`, and derived identities against the migration ledger before running the live adapter path.
- Preserve controller/server versus SDK-worker authority, opaque completion capabilities, reservations, quarantine, bounded cleanup, diagnostics, and credential secrecy.
- Rewrite tests that imported old runtime internals through public prepared-plan views or move genuinely core assertions to the core corpus; do not export internals.
- Rename active Testpilot tests/selectors and update existing Make/workflow coverage so legacy Umpire2/3 failures and the new success test remain selected exactly.

### Investigation targets
**Required** (read before coding):
- `tools/umpire/temporal/host.go:23-109` — composite authority boundary
- `tools/umpire/temporal/server/host.go:21-120` — controller/server authority
- `tools/umpire/temporal/worker/host.go:20-136` — worker authority and cleanup
- `tests/testcore/test_env.go:47-88` — functional environment owner
- `tests/umpire4_async_nexus_case_test.go:26-124` — live consumer
- `Makefile:1107-1133` — aggregate selectors and inherited identities

### Acceptance

## Acceptance
- [ ] Functional Driver, delivery, server, worker, composite, fixtures, tests, and READMEs live under testcore; the former Temporal adapter path is absent and common Testpilot does not import testcore.
- [ ] Separate server/worker authorities, Driver identity/preflight, completion capability secrecy, reservations, cancellation, cleanup/quarantine, diagnostics, and failure precedence match the frozen corpus.
- [ ] Former internal-import tests use supported public views or core-owned tests without widening Testpilot; no functional or future canary Driver can replace Contract evaluation or recorded evidence.
- [ ] The Testpilot fixtures generated by task 5 remain at their final destination and the superseded Umpire fixtures are removed with the old adapter; every namespace-derived difference matches the task-1 allowlist and migration ledger before focused and live verification.
- [ ] Focused adapter tests and the real live Testpilot success path pass; Make/workflow selectors include all moved packages/tests and preserve the exact inherited Umpire2/3 failure set without silent coverage loss.

## Done summary
Moved the complete functional Temporal Driver, delivery ledger, server/worker authorities, lifecycle tests, READMEs, fixtures, and live success consumer from `tools/umpire/temporal` into `tests/testcore/testpilot`. The former adapter and Umpire fixture copies are absent; task-5 Testpilot fixture hashes remain exact; the live Testpilot path passes while the aggregate selector preserves the inherited Umpire2/Umpire3 failure identities.

Internal-import adapter tests now use public prepared views, while the private async-Nexus correlation mutation and live/offline parity matrix lives under common Testpilot verification. Make and workflow selectors cover `./tools/umpire/...`, `./common/testing/testpilot/...`, and `./tests/testcore/testpilot/...`. The remaining Umpire conformance consumer uses the moved descriptor closure during coexistence without changing task-7 protocol or corpus ownership.

The pre-edit conformance checks were red only for seven generated `NamedType.protobufType` substitutions caused by task 5's shared producer identity cutover: two in cleanup-failure-after-proved-violation and one in each other class. Task 7 explicitly owns regenerating and moving that six-class corpus; the exact diff and ownership are recorded in the migration ledger.

Full `make lint-code` reached analysis without a lint finding but could not complete because transient Go typecheck output exhausted the local disk, including after generated-cache cleanup and a no-fix retry. No tracked formatter mutation occurred, all focused/owner/live gates passed, and `git diff --check` passed.

stage: impl-review - ran | SHIP after two valid P2 fixes
stage: plan-sync - skipped(config: planSync.enabled != true)
## Evidence
- Commits:
- Tests: GOFLAGS='-p=1' CGO_ENABLED=0 TMPDIR=/private/tmp go test -count=1 -tags test_dep ./common/testing/testpilot/..., GOFLAGS='-p=1' CGO_ENABLED=0 TMPDIR=/private/tmp go test -count=1 -tags test_dep ./tests/testcore/testpilot/..., GOFLAGS='-p=1' CGO_ENABLED=0 TMPDIR=/private/tmp go test -count=1 -tags test_dep ./tools/umpire -run '^TestCaseRuntimePublicFacadeConformance$', GOFLAGS='-p=1' CGO_ENABLED=0 TMPDIR=/private/tmp go test -count=1 -tags test_dep ./tools/umpire/regression -run '^(TestUmpireCIWorkflowRunsSeparatedUnitAndLiveProofs|TestUmpireDocumentationStatesAttachedOwnershipAndBoundedClaim)$', GOFLAGS='-p=1' CGO_ENABLED=0 TMPDIR=/private/tmp mise exec -- go test -count=1 -tags test_dep ./tools/umpire/... ./common/testing/testpilot/... ./tests/testcore/testpilot/..., GOFLAGS='-p=1' CGO_ENABLED=0 TMPDIR=/private/tmp mise exec -- go test -count=1 -tags 'test_dep integration' ./tests -run '^TestTestpilotAsyncNexusCase$', GOFLAGS='-p=1' CGO_ENABLED=0 TMPDIR=/private/tmp make umpire-check-live-tests, git diff --check, INHERITED_RED: make umpire-check-case-runtime-conformance - seven task-7-owned NamedType namespace substitutions after task-5 producer cutover, TOOLING_RED: GOLANGCI_LINT_FIX=false GOFLAGS='-p=1' CGO_ENABLED=0 TMPDIR=/private/tmp make lint-code - transient Go typecheck output exhausted local disk after analysis; no lint finding
- PRs:
