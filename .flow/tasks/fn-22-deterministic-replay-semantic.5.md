---
satisfies: [R4]
---
# fn-22-deterministic-replay-semantic.5 Compile ordered typed edits into whole candidate Cases through a Lean replay bridge

## Description
Add `Umpire.Replay`: over an `AdmittedQuery` with its checked Model, the finite ordered edit `dropPrefixStep i` (last prefix step first) defined over the Scenario's action sequence (`Scenario.exactly` minus occurrence `i`, the behavior author rebuilt from the set's declared Scenario; an exploration Query drops the step from `traceExactly` too), each re-admitted through `Umpire.Command.checkAdmitted` and reported `inapplicable` when the Model does not admit it, and a monotonic `Reduction` value that retains a candidate, never retries a rejected edit and never reintroduces a dropped step. Lift the exploration bridge's frame parsing, rejection rules, profile echo and `Effects` into `Temporal.Tool.Bridge`, imported by both executables, with `umpire-check-exploration-bridge` kept green. Add `Temporal.Tool.ReplayBridge` (`umpire-replay-bridge`, non-default): `admit` names the set and the Query or the exploration target key, recovers the admitted Query through the bridge's typed binding table (the Model, its sets, their Queries and the realization each `case` block names, as the exploration bridge's `Binding`; or the fn-33 campaign replanned to that target), re-produces the Case under the set's realization and admits only when the compact canonical bytes are the subject's; `next` hands out the next candidate as one whole Case under `temporal.case.<set>.<digest>` with the fixture `<set>-<digest>`, the digest being the edited Query's Plan checksum (`Umpire.Exploration.candidateDigest`, computed before the Case exists, as the exploration bridge names a candidate; the subject's own digest is its admitted Query's Plan checksum), with the edit applied, or reports the edit `rejected` with the Producer's reason; the Case checksum travels beside it as the identity and names nothing; `observe` takes the candidate's classification (task .4's class, by Go) and advances the reduction; `finish` reports minimized, irreducible or incomplete with every edit's fate. Determinism as fn-33 .5 pins it: the same frames twice give the same frames.

### Approach
- The lamp Model of `Umpire.Exploration.Tests.Classed` and the switch pin the edits; the caller Model's exploratory candidate pins `irreducible` at once; a functional caller Query with a prefix pins one admitted edit.

### Quick commands
`cd model && lake build umpire-replay-bridge umpire-replay-bridge-tests umpire-explore umpire-explore-tests UmpireTests && lake exe umpire-replay-bridge-tests && cd .. && make umpire-check-exploration-bridge`

**Size:** L
**Files:** `model/Umpire/Replay.lean`, `model/Umpire/Replay/Edits.lean`, `model/Umpire/Replay/Tests.lean`, `model/Temporal/Tool/Bridge.lean`, `model/Temporal/Tool/ExplorationBridge.lean`, `model/Temporal/Tool/ExplorationBridgeTests.lean`, `model/Temporal/Tool/ReplayBridge.lean`, `model/Temporal/Tool/ReplayBridgeMain.lean`, `model/Temporal/Tool/ReplayBridgeTests.lean`, `model/lakefile.lean`, `Makefile`
**Touches:** `model/Umpire/Replay*`, `model/Temporal/Tool/**`, `model/lakefile.lean`, `Makefile`

### Re-plan note (2026-09-22)
Rewritten on fn-85, fn-86, fn-87 and fn-33 after the first plan's MAJOR_RETHINK and revised through the six plan review rounds the spec's **Plan review** section records (SHIP on round six, 2026-09-22).
## Acceptance
- [x] Lean owns applicability, order, candidate naming (the Plan checksum digest) and Case compilation, with the Case checksum reported beside it; an edit the Model does not admit is `inapplicable`, recorded, and produces no Case; no edit is listed twice.
- [x] A subject whose bytes no set of the Model produces is `crossed` at `admit`; duplicate, stale, out-of-order and oversized frames are rejected before any campaign call; the exploration bridge's tests and gate stay green over the shared module.
- [x] Go receives whole Cases and returns classes; it has no Case mutation or coordinate-editing API.
## Done summary
`Umpire.Replay` owns the edit `dropPrefixStep i` (`Umpire.Replay.Edits`: `editable` reads the Scenario's exact action sequence, `sweepOf` orders the prefix steps last first, `restrict` keeps positions through `Scenario.withSteps` and shortens an exact trace alike, `restrictSource` edits a `QuerySource`) and the monotonic `Reduction` (at most eight edits, `settle` never brings a dropped step back or re-tries an edit, both proved as `Sublist` lemmas; `result` is `minimized`, `irreducible` or `incomplete`). `admitKept` re-admits the edited Query and names it by its Plan checksum; `Admitted.produce` produces its whole Case. The `query` command now emits `<query>.source` (`Umpire.Command.QuerySource`, whose `admit` is the block's own admission) for a one-instance Query, and a functional `case` block names each Case's `claims`, `catalog` and `relations` beside it, so the bridge re-produces exactly what the block produced. `Temporal.Tool.Bridge` carries what both bridges share (effects, envelope parsing with closed keys, sequence and Profile rejections, the oversized-line rejection, rendering helpers, `serve`); the exploration bridge is rebuilt on it with its tests and gate green. `Temporal.Tool.ReplayBridge` (`umpire-replay-bridge`, non-default) serves `admit` / `next` / `observe` / `finish` over a typed binding table (the negative control's set, the caller's functional set's reducible Queries, the caller's exploratory set by target key): `admit` re-produces the subject and answers `crossed` unless the SHA-256 of the compact canonical bytes is the subject's; `next` hands out one whole Case under `temporal.case.<set>.<digest>` with its Case checksum beside it, listing every `inapplicable` or `rejected` edit passed over; `observe` takes a class or a preparation rejection; `finish` reports the result and every edit's fate. Pinned in `Umpire.Replay.Tests` (the lamp's admitted edit, the switch's empty sweep, the reduction on names) and `umpire-replay-bridge-tests` (the control irreducible at once, a crossed subject, the caller's `scheduleToStartTimeout` minimized by dropping the worker stop and driven through every class, an exploration subject irreducible at once, every rejection, determinism, the built executable), gated by `make umpire-check-replay-bridge`.

## Evidence
- Commits:
- Tests:
- PRs:
