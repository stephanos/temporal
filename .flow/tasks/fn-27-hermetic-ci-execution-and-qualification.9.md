# fn-27-hermetic-ci-execution-and-qualification.9 Complete the CI regression matrix and documentation

## Description
Run focused and aggregate Lean, Go, generated-view, and regression checks. Update contributor and architecture documentation with the exact ordinary CI command, byte-identical v2 subject, shared runner/Run Evaluation path, stable parity claim, and exclusions. Preserve existing comments, rewording only replaced Umpire vocabulary.

## Acceptance
- [ ] Focused and aggregate checks pass with deterministic results.
- [ ] Docs state the exact portability claim and distinguish stable meaning from runtime transport identities.
- [ ] Docs make clear that Evaluation Profiles, Evaluation Receipts, provenance schemas, new artifact-set versions, Claim Assessment, remote, canary, and release work are excluded.

## Done summary
Documented the exact focused and aggregate hermetic CI commands in the contributor and architecture surfaces, including the byte-identical v2 input, shared runner and Run Evaluation path, stable typed semantic parity, allowed transport-identity differences, and every explicit exclusion. Added a focused documentation contract regression; all four Quick commands are green, nonmutating diff lint reports zero task issues with only the inherited `tools/umpire/runtime/errors.go:60` errortype warning, and review returned SHIP with no findings.

stage: impl-review - ran (model: gpt-5.6-sol)
## Evidence
- Commits: e21e07b0e02a498d60a4572dc68ee8107518cdaa
- Tests: baseline: green - cd model && mise exec -- lake build Temporal.Tool.RunEvaluationTests (/tmp/fn27-9-baseline-lean.Ej70He), baseline: green - GOFLAGS=-tags=test_dep CC=$(xcrun --find clang) CXX=$(xcrun --find clang++) SDKROOT=$(xcrun --show-sdk-path) TMPDIR=$(pwd -P)/.flow/tmp/go-tmp mise exec -- go test -count=1 ./tools/umpire/runtime/... ./tools/umpire/runevaluation/... ./tools/umpire/temporal/nexus/... (/tmp/fn27-9-baseline-go.a1R8O7), baseline: green - GOFLAGS=-tags=test_dep CC=$(xcrun --find clang) CXX=$(xcrun --find clang++) SDKROOT=$(xcrun --show-sdk-path) TMPDIR=$(pwd -P)/.flow/tmp/go-tmp mise exec -- go test -count=1 ./tools/umpire/temporal/nexus/... -run '^TestHermeticCIPortability$' (/tmp/fn27-9-baseline-focused.72xfsz), baseline: green - GOFLAGS=-tags=test_dep CC=$(xcrun --find clang) CXX=$(xcrun --find clang++) SDKROOT=$(xcrun --show-sdk-path) TMPDIR=$(pwd -P)/.flow/tmp/go-tmp mise exec -- make umpire-check-regression (/tmp/fn27-9-baseline-regression.KWC0HI), STRICT_RED: GOFLAGS=-tags=test_dep CC=$(xcrun --find clang) CXX=$(xcrun --find clang++) SDKROOT=$(xcrun --show-sdk-path) TMPDIR=$(pwd -P)/.flow/tmp/go-tmp mise exec -- go test -count=1 ./tools/umpire/regression -run '^TestHermeticCIDocumentationStatesBoundedClaim$' (failed before docs update: exact bounded claim absent; /tmp/fn27-9-strict-red.71a3Gk), GOFLAGS=-tags=test_dep CC=$(xcrun --find clang) CXX=$(xcrun --find clang++) SDKROOT=$(xcrun --show-sdk-path) TMPDIR=$(pwd -P)/.flow/tmp/go-tmp mise exec -- go test -count=1 ./tools/umpire/regression -run '^TestHermeticCIDocumentationStatesBoundedClaim$' (/tmp/fn27-9-doc-green.YV9gLs), cd model && mise exec -- lake build Temporal.Tool.RunEvaluationTests (/tmp/fn27-9-verify-lean.vKpBk8), GOFLAGS=-tags=test_dep CC=$(xcrun --find clang) CXX=$(xcrun --find clang++) SDKROOT=$(xcrun --show-sdk-path) TMPDIR=$(pwd -P)/.flow/tmp/go-tmp mise exec -- go test -count=1 ./tools/umpire/runtime/... ./tools/umpire/runevaluation/... ./tools/umpire/temporal/nexus/... (/tmp/fn27-9-verify-go.akXYN4), GOFLAGS=-tags=test_dep CC=$(xcrun --find clang) CXX=$(xcrun --find clang++) SDKROOT=$(xcrun --show-sdk-path) TMPDIR=$(pwd -P)/.flow/tmp/go-tmp mise exec -- go test -count=1 ./tools/umpire/temporal/nexus/... -run '^TestHermeticCIPortability$' (/tmp/fn27-9-verify-focused.7PB3fW), GOFLAGS=-tags=test_dep CC=$(xcrun --find clang) CXX=$(xcrun --find clang++) SDKROOT=$(xcrun --show-sdk-path) TMPDIR=$(pwd -P)/.flow/tmp/go-tmp mise exec -- make umpire-check-regression (/tmp/fn27-9-verify-regression.w3PI2E), GOLANGCI_LINT_BASE_REV=98ebee3a7f813ef55b5e40c2fff9eae3b12de1de GOLANGCI_LINT_FIX=false mise exec -- make lint-code (0 task-diff issues; inherited tools/umpire/runtime/errors.go:60 et:unw+ failure; /tmp/fn27-9-lint.RMFufb), git diff --check, GATE_SKIPPED:build:green-receipt e21e07b0 - final verification reused prior post-edit pass, GATE_SKIPPED:unittest:green-receipt e21e07b0 - final verification reused prior post-edit pass, GATE_SKIPPED:smoke:green-receipt e21e07b0 - final verification reused prior post-edit pass, GATE_SKIPPED:regression:green-receipt e21e07b0 - final verification reused prior post-edit pass
- PRs: