// Package profile defines deployment authority and binds it to realized Environment capabilities.
// Binding may narrow authority and capabilities but must never broaden either one.
package deployment
