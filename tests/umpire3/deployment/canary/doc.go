// Package canary executes approved experiments within a durable, killable production safety envelope.
// Approval, profile, and experiment identities are immutable, and incomplete cleanup is inconclusive.
package canary
