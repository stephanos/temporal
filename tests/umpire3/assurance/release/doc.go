// Package qualification binds external execution evidence to a candidate release and deployment deployment.
// It emits a receipt only when identities, trust, capabilities, faults, and cleanup all satisfy policy.
package release
