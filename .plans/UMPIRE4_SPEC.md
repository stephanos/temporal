# Umpire Spec

These are the most important rules and invariants for Umpire.
They must not be violated; a human must be consulted when breaking any of them or planning to break any of them.

## Model

L1. all model code is in Lean programming language
L2. all Lean code is in model/

## CLI

C1. all CLI code is in tools/umpire (or imported from temporal/tools/common)
